# LearnLoop-AI v0.4.0 功能测试文档

> 适用版本：v0.4.0 | 测试方式：前端操作 + API 直调 + 数据库验证  
> 前置条件：后端服务已启动（`:8000`），前端已启动（`:8501`），`.env` 中已配置 `DEEPSEEK_API_KEY`

---

## 目录

1. [环境准备](#1-环境准备)
2. [测试 1：笔记生成 + SM-2 联动](#2-测试-1笔记生成--sm-2-联动)
3. [测试 2：笔记管理](#3-测试-2笔记管理)
4. [测试 3：文件上传入库](#4-测试-3文件上传入库)
5. [测试 4：出题 + 做题 + 批改](#5-测试-4出题--做题--批改)
6. [测试 5：错题本](#6-测试-5错题本)
7. [测试 6：RAG 知识问答](#7-测试-6rag-知识问答)
8. [测试 7：学习仪表盘](#8-测试-7学习仪表盘)
9. [测试 8：SM-2 复习评分](#9-测试-8sm-2-复习评分)
10. [测试 9：Memory 薄弱点与混淆对](#10-测试-9memory-薄弱点与混淆对)
11. [测试 10：API 直接验证](#11-测试-10api-直接验证)
12. [回归检查清单](#12-回归检查清单)

---

## 1. 环境准备

### 1.1 启动服务

**终端 1 — 后端：**
```bash
cd backend
..\venv\Scripts\activate.bat
python -m app.main
```
预期输出：
```
============================================================
>> AI Study Agent v0.4.0 启动中...
[OK] SQLite database initialized
[OK] LLM Providers available: deepseek
[OK] Registered 6 Agents
API Docs: http://0.0.0.0:8000/docs
============================================================
```

**终端 2 — 前端：**
```bash
cd frontend
..\venv\Scripts\activate.bat
streamlit run streamlit_app.py
```

### 1.2 验证服务可用

- 浏览器打开 http://localhost:8000/docs — 应看到 27 个 API 端点
- 浏览器打开 http://localhost:8501 — 侧边栏应显示 ✅ 后端已连接 (6 Agents)
- 版本号应显示 v0.4.0

### 1.3 准备测试数据

创建一个测试用的 txt 文件（比如 `测试文档.txt`）：

```
软件测试基础

软件测试是验证和确认软件质量的过程。主要测试方法包括：

1. 黑盒测试：不关注内部实现，只关注输入输出。包括等价类划分、边界值分析、决策表测试。
2. 白盒测试：关注内部逻辑结构。包括语句覆盖、分支覆盖、路径覆盖。
3. 灰盒测试：结合黑盒和白盒的方法。

Verification（验证）是确保产品符合规格说明，回答"我们正确地建造产品了吗"。
Validation（确认）是确保产品满足用户需求，回答"我们建造了正确的产品吗"。

常见的测试级别包括单元测试、集成测试、系统测试、验收测试。
```

---

## 2. 测试 1：笔记生成 + SM-2 联动

### 2.1 前端生成笔记

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 前端侧边栏选择 **📝 生成笔记** | 显示表单：学习主题 + 笔记风格 + 补充内容 |
| 2 | 主题栏输入 `软件测试方法概述` | |
| 3 | 风格选 `detailed` | |
| 4 | 补充内容粘贴测试文档的文字 | |
| 5 | 点击 **🚀 生成笔记** | 显示 spinner → 返回结构化 Markdown 笔记 |
| 6 | 检查提示 | 应显示 "✅ 笔记已保存到知识库" |
| 7 | 检查笔记内容 | 含标题层级、摘要、标签、章节数 |

### 2.2 验证 SM-2 状态自动创建

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 浏览器打开 http://localhost:8000/api/v1/schedule/states | 返回 JSON，`data` 数组中应有知识点 `软件测试方法概述` |
| 2 | 检查初始值 | `ef: 2.5`，`interval_days: 1`，`repetitions: 0` |

### 2.3 负面测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 主题栏留空，点击生成 | 按钮应为 disabled 状态 |
| 2 | 停止后端，点击生成 | 前端显示 "❌ 无法连接到后端" |

---

## 3. 测试 2：笔记管理

### 3.1 笔记列表

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 侧边栏选择 **📚 我的笔记** | 显示笔记列表，刚才生成的笔记应在第一条 |
| 2 | 检查列表项 | 显示标题、摘要（截断）、标签徽章、日期、字数、"🤖 AI生成" |
| 3 | 检查分页信息 | 底部显示"共 X 篇笔记" |

### 3.2 笔记搜索

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 搜索框输入 `软件` | 过滤出标题/内容含"软件"的笔记 |
| 2 | 来源过滤选 `AI生成` | 只显示 source_type=generated 的笔记 |
| 3 | 点击 **🔄 重置** | 恢复全部列表 |

### 3.3 笔记详情

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击某篇笔记的 **📖 查看** | 跳转到详情视图 |
| 2 | 检查详情 | 显示完整 Markdown 内容、字数、日期、标签、来源 |
| 3 | 点击 **← 返回笔记列表** | 回到列表页 |

### 3.4 笔记删除

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 进入笔记详情 | |
| 2 | 点击 **🗑️ 删除此笔记** | 显示确认提示"确定要删除这篇笔记吗？" |
| 3 | 点击 **✅ 确认删除** | 删除成功，返回列表 |
| 4 | API 验证 | 同名笔记不再出现在 `/api/v1/notes` 里 |
| 5 | ChromaDB 验证 | knowledge_chunks 中该 note_id 的 chunks 已被清除 |

---

## 4. 测试 3：文件上传入库

### 4.1 上传文件

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 侧边栏选择 **📁 知识库管理** | 显示统计卡片 + 上传区域 |
| 2 | 点击 **Browse files**，选择之前创建的 `测试文档.txt` | 显示文件名和大小 |
| 3 | 自定义标题填 `测试文档_上传版` | |
| 4 | 点击 **🚀 上传并入库** | spinner → "✅ 文档已入库" |
| 5 | 检查下方列表 | 出现刚上传的文档，标记"📤 上传" |

### 4.2 验证上传链路

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开 http://localhost:8000/api/v1/rag/stats | `uploaded_notes` 数 +1 |
| 2 | 进入笔记列表 → 来源过滤选"上传的" | 能看到上传的文档 |
| 3 | 进入 RAG 问答页，搜 `黑盒测试包括哪些` | 应该能基于上传文档的内容回答（因为上传后也建立了向量索引） |

### 4.3 删除上传文档

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在知识库管理页，点击文档的 **🗑️ 删除** | 文档从列表消失 |
| 2 | 去笔记列表确认 | 文档也不再出现 |

### 4.4 边界测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 上传 `.exe` 文件 | 提示"不支持的文件类型" |
| 2 | 上传空文件 | 提示"文件内容为空" |
| 3 | 上传无文字的 PDF（如果有的话） | 报错"未提取到文本内容" |

---

## 5. 测试 4：出题 + 做题 + 批改

### 5.1 生成题目

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 侧边栏选 **🎯 出题练习** | 显示表单 |
| 2 | 主题填 `软件测试基础` | |
| 3 | 难度选 `medium` | |
| 4 | 数量选 `5` | |
| 5 | 题型勾选 `选择题` + `判断题` | |
| 6 | 点击 **🎯 生成题目** | 返回题目列表，每题有题号、题目、类型标签、选项 |

### 5.2 做题（前端选题）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 对每道选择题，点选一个选项 | radio 选中 |
| 2 | 展开 **查看答案** | 显示正确答案和解析 |

> 注意：当前前端做题体验是"自测模式"——选题后自己点开答案对。正式做题需要后端 `/quiz/{id}/submit` API。

### 5.3 API 做题 + 批改（关键链路）

**用 curl 或 Postman 测试：**

```bash
# Step 1: 生成题目
curl -X POST http://localhost:8000/api/v1/quiz/generate \
  -H "Content-Type: application/json" \
  -d '{"topic":"黑盒测试方法","types":["choice"],"difficulty":"medium","count":3}'

# 记录返回的 quiz_id（例如 quiz_abc123）
```

```bash
# Step 2: 提交答案（故意错几道）
curl -X POST http://localhost:8000/api/v1/quiz/{quiz_id}/submit \
  -H "Content-Type: application/json" \
  -d '{
    "quiz_id":"quiz_abc123",
    "answers":[
      {"question_id":"q_001","answer":"C"},
      {"question_id":"q_002","answer":"A"},
      {"question_id":"q_003","answer":"随便写个错的"}
    ]
  }'
```

预期返回：
```json
{
  "success": true,
  "data": {
    "total_score": ...,
    "results": [
      {"question_id": "q_001", "is_correct": ..., "score": ..., "feedback": "..."},
      ...
    ]
  }
}
```

| 验证项 | 检查方式 | 预期 |
|--------|---------|------|
| Quiz 入库 | 打开 http://localhost:8000/api/v1/quiz/history | 能看到刚才的 quiz |
| 做题记录入库 | 数据库 `quiz_attempts` 表 | 有一条新记录 |
| 错题入库 | http://localhost:8000/api/v1/quiz/errors/list | 答案错误的题目出现在这里 |
| SM-2 联动 | http://localhost:8000/api/v1/schedule/states | 错题的知识点已创建 SM-2 状态 |
| 混淆对联动 | http://localhost:8000/api/v1/memory/confusions | 同一批错题的不同知识点之间有混淆对 |

---

## 6. 测试 5：错题本

### 6.1 错题展示

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 侧边栏选 **📋 错题本** | 显示统计卡片（总错题/未解决/已掌握） |
| 2 | 检查错题列表 | 按知识点分组，每题显示"你的答案 vs 正确答案" |
| 3 | 状态标识 | 未解决的标记 ❌ 待复习，已掌握的标记 ✅ 已掌握 |

### 6.2 标记已掌握

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在某道未解决的错题下点击 **✅ 我已掌握** | 显示"已标记为掌握！" |
| 2 | 检查该题状态 | 变为 ✅ 已掌握，卡片变绿色 |
| 3 | 使用过滤 | 选"已掌握"只看到已解决的，选"未解决"只看到未解决的 |

### 6.3 空状态

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 全新数据库，无错题 | 显示"🎉 做得很棒！目前没有错题记录" |

---

## 7. 测试 6：RAG 知识问答

### 7.1 基础问答

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 侧边栏选 **🔍 知识问答** | 显示输入框 |
| 2 | 输入 `什么是黑盒测试`，检索数量保持 5 | |
| 3 | 点击 **🔍 提问** | 返回答案 + 参考来源列表 |
| 4 | 检查来源 | 显示笔记标题 + 相关度分数 + 引用片段 |
| 5 | 检查检索信息 | 底部显示"检索到 N 个文档块" + 置信度 |

### 7.2 Multi-Query + Rerank 验证

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 提问 `V&V 的区别` | 底部应出现 "🔀 查询扩展: +N 变体" 和 "📊 Rerank 已启用" |
| 2 | 检查后端日志 | 应打印 `[RAG] Query expanded: N queries` |

### 7.3 知识库无相关内容

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 提问一个知识库里绝对没有的主题（如 `如何养猫`） | 回答应明确告知"知识库中暂无相关信息" |

---

## 8. 测试 7：学习仪表盘

### 8.1 仪表盘加载

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 侧边栏选 **📊 学习仪表盘**（排第一个） | 页面加载，四张统计卡片有数值 |
| 2 | 检查卡片 | 🔥 连续学习天数、📅 待复习数量、📝 总做题次数、📈 掌握率 |

### 8.2 待复习任务

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 检查"今日待复习任务"区域 | 到期知识点列表，按红🔴/黄🟡/绿🟢标记优先级 |
| 2 | 验证排序 | 错误次数多 + 逾期天数长的排前面 |

### 8.3 知识点进度条

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 检查"知识点掌握分布"区域 | 每个知识点显示进度条（EF 值映射） |

### 8.4 空状态

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 全新数据库打开仪表盘 | 待复习显示"🎉 今天没有到期的复习任务！"，知识点无数据时提示"去生成笔记" |

---

## 9. 测试 8：SM-2 复习评分

### 9.1 复习页面

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 侧边栏选 **📅 复习计划** | 加载所有 SM-2 知识点 |
| 2 | 检查三个 Tab | 🔴 需重点复习（有错题的）、🟢 正常复习（没错题的）、📋 全部知识点 |
| 3 | 确认排序 | 有错题的知识点出现在"需重点复习"tab 里 |

### 9.2 评分功能

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在"需重点复习"tab，选择一个知识点 | 看到 EF、间隔、已复习次数、错题数 |
| 2 | 拖动 slider 选评分（如 4） | label 变为"当前评分: 4" |
| 3 | 点击 **✅ 提交评分** | 显示 ✅ 成功，展示：下次复习日期、新间隔、EF 值 |
| 4 | 检查 EF 变化 | 评分 4-5 → EF 不变或微升，评分 0-2 → EF 下降 |

### 9.3 评分后数据验证

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 刷新页面 | 该知识点的 EF/间隔/下次复习日期 已更新 |
| 2 | 打开 http://localhost:8000/api/v1/schedule/states | 确认该知识点的数据已持久化 |
| 3 | 回到仪表盘，待复习数可能变化 | 如果评了高分，该知识点不再出现在"今日待复习"中 |

---

## 10. 测试 9：Memory 薄弱点与混淆对

### 10.1 薄弱点分析

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 浏览器打开 http://localhost:8000/api/v1/memory/weak-points | 返回薄弱点数据 |
| 2 | 检查返回 | `weak_points` 按错误次数排序，`by_type` 有错误分类统计 |

### 10.2 混淆对列表

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 浏览器打开 http://localhost:8000/api/v1/memory/confusions | 返回混淆对列表 |
| 2 | 确认数据 | 做测试 4 时的错题知识点两两之间出现了混淆对记录 |

### 10.3 学习报告

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 浏览器打开 http://localhost:8000/api/v1/memory/report | 返回综合报告 |
| 2 | 检查内容 | 含统计分析 + LLM 生成的建议（如果有 DeepSeek Key） |

---

## 11. 测试 10：API 直接验证

不经过前端，直接验证以下端点返回 200：

```bash
# 设置 BASE
BASE=http://localhost:8000/api/v1

# 系统
curl $BASE/../                                                     # 系统信息
curl $BASE/../health                                               # 健康检查

# 笔记
curl "$BASE/notes?limit=5"                                         # 笔记列表
curl "$BASE/notes/search?query=软件&source_type=generated"          # 搜索
curl "$BASE/notes/{note_id}"                                       # 笔记详情

# 题目
curl "$BASE/quiz/history?limit=5"                                  # 做题历史
curl "$BASE/quiz/errors/list?limit=20"                             # 错题列表

# 知识检索
curl "$BASE/rag/stats"                                              # 统计
curl "$BASE/rag/sources?limit=10"                                   # 文档列表

# 学习规划
curl "$BASE/schedule/daily"                                         # 每日任务
curl "$BASE/schedule/stats"                                         # 统计
curl "$BASE/schedule/states"                                        # SM-2 状态
curl "$BASE/schedule/plans"                                         # 学习计划列表

# 记忆
curl "$BASE/memory/weak-points"                                     # 薄弱点
curl "$BASE/memory/confusions"                                      # 混淆对
curl "$BASE/memory/error-log?limit=20"                              # 错题记录
```

**全部应返回 `{"success":true,...}`，状态码 200。**

---

## 12. 回归检查清单

每次改完代码后，快速跑一轮：

### 核心链路（必测）

- [ ] 生成一篇笔记 → 前端看到内容 + 提示已入库
- [ ] 上传一个文件 → 知识库列表出现
- [ ] 生成一套题目 → submit API 提交答案
- [ ] 错题本出现错题
- [ ] 仪表盘显示统计数据
- [ ] 复习页面对知识点评分 → EF/间隔更新
- [ ] RAG 搜一个已知主题 → 返回带来源的回答

### 崩坏测试

- [ ] 停止后端 → 前端所有页面显示连接错误而非白屏
- [ ] 后端启动时无 .env → 打印 WARN 但不崩溃
- [ ] 删除笔记 → 列表中消失 + ChromaDB 清除
- [ ] 无笔记时 RAG 提问 → 返回"暂无相关信息"

### 数据一致性

- [ ] 笔记生成后，`/notes`、`/rag/stats`、`/schedule/states` 数据一致
- [ ] 做题后，`/quiz/history`、`/quiz/errors/list`、`/schedule/states`、`/memory/confusions` 数据一致
- [ ] 复习评分后，`/schedule/states` 的 EF/间隔/下次复习日期已更新

---

> **测试周期建议**
>
> - 每次加新功能后：跑完整轮一遍（约 15 分钟）
> - 每次改老功能后：跑相关模块 + 核心链路回归（约 5 分钟）
> - 每次发版前：全量 12 个测试 + API 全部端点验证（约 30 分钟）
